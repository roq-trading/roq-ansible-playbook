# Copyright (c) 2017-2018, Hans Erik Thrane

{% if femas.md_user is defined and femas.md_user %}
front = "{{ femas.md_user.front }}"
{% if femas.md_user.name_server is defined %}
name_server = "{{ femas.md_user.name_server }}"
{% endif %}

account = "{{ femas.md_user.account }}"
password = "{{ femas.md_user.password }}"
broker = "{{ femas.md_user.broker }}"

topics = {
  DIALOG  = QUICK
  PRIVATE = QUICK
  PUBLIC  = QUICK
  QUERY   = QUICK
  QUOTE   = QUICK
{% if femas.md_user.topics is defined %}
{% for topic in femas.md_user.topics %}
  {{ topic }} = QUICK
{% endfor %}
{% endif %}
}
{% endif %}
