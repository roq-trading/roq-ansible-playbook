---

- name: combine user configuration with defaults
  set_fact:
    roq_coinbase_pro_config: >
        {{ roq_coinbase_pro_defaults | combine(roq_coinbase_pro | default({}), recursive=True) }}
    cacheable: true
  tags:
    - always

- name: configuration (debug)
  debug:
    var: config
    verbosity: 1
  tags:
    - always

- name: shorter variable name for config
  set_fact:
    config: "{{ roq_coinbase_pro_config }}"
  tags:
    - roq-coinbase-pro

- block:

  - name: create required directories
    file:
      path: "{{ root }}/{{ item }}"
      state: directory
      owner: "{{ roq_user }}"
      # group: "{{ roq_user }}"
      mode: 0770
    with_items:
      - "/etc/roq"
      - "/var/lib/roq"
      - "/var/lib/roq/coinbase-pro"
      - "/var/log/roq"
      - "/var/run/roq"

  - name: create config files
    template:
      src: "templates/{{ item | basename }}"
      dest: "{{ root }}/etc/roq/{{ item }}"
      owner: "{{ roq_user }}"
      group: "{{ roq_user }}"
      mode: 0644
    with_items:
      - "roq-coinbase-pro.toml"
      - "roq-coinbase-pro.gflags"

  - name: create run scripts
    template:
      src: "templates/roq-coinbase-pro.sh"
      dest: "{{ root }}/usr/local/bin/roq-coinbase-pro.sh"
      owner: "{{ roq_user }}"
      # group: "{{ roq_user }}"
      mode: 0750

  - name: install the roq-coinbase-pro conda package
    shell: "{{ root }}/miniconda/bin/conda install -y -n base \
      roq-coinbase-pro{{ '' if config.version == 'latest' else '==%s'|format(config.version) }}"
  
  when: "'roq_coinbase_pro' in group_names"
  become: "{{ 'true' if become_user is defined else 'false' }}"
  tags:
    - roq-coinbase-pro

##############
### SERVER ###
##############

- block:

  - name: docker pull images
    docker_image:
      name: "{{ docker_registry_prefix | default('') }}{{ config.docker.name }}"
      tag: "{{ config.version }}"
      source: pull
      force_source: true
      force_tag: true
      build:
        http_timeout: 600
    when: config.docker.use

  - name: define list of systemd services
    set_fact:
      systemd_services:
      - 'roq-coinbase-pro.service'
      - 'roq-coinbase-pro-stop.service'

  - name: define list of systemd timers
    set_fact:
      systemd_timers:
      - 'roq-coinbase-pro.timer'
      - 'roq-coinbase-pro-stop.timer'

  - name: define list of systemd units
    set_fact:
      systemd_units:
      - '{{ systemd_services }} + {{ systemd_timers }}'

  - name: create systemd units
    template:
      src: "templates/{{ item }}"
      dest: "/etc/systemd/system/{{ item }}"
      owner: root
      group: root
      mode: 0644
    with_items: "{{ systemd_units }}"

  - name: enable systemd units
    systemd:
      name: "{{ item }}"
      enabled: false
      daemon_reload: true
    with_items: "{{ systemd_units }}"

  - name: start systemd timers
    systemd:
      name: "{{ item }}"
      state: started
    with_items: "{{ systemd_timers }}"

  when: become_user is defined and 'roq_coinbase_pro' in group_names
  become: true
  tags:
    - roq-coinbase-pro
