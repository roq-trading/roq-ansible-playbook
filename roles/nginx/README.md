# nginx

Install Nginx.

## Requirements

None

## Variables

* `nginx_listen` (str)
  * "0.0.0.0" (default)

## Tasks

* Create directories
* Create a top-level index.html
* Create configuration
* Pull latest Docker image
* Create systemd services
