[Unit]
Description=Femas metrics bridge -- unix to tcp
After=network-online.target

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker run --rm --name %n -i \
  --network roq-network \
  --volume {{ root }}/var/tmp:/var/tmp \
  roq-datafeed-bridge \
  /usr/bin/socat tcp-listen:{{ datafeed_config.metrics.port }},fork unix-connect:/var/tmp/datafeed.metrics.sock

[Install]
WantedBy=multi-user.target
