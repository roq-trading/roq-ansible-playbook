--name={{ config.name }}
--config-file={{ root }}/etc/roq/roq-simulator.toml
--listen={{ root }}/var/tmp/roq-simulator.sock
--metrics={{ root }}/var/tmp/roq-simulator_metrics.sock
{% for (key, value) in config.cpu_affinity.items() %}
--{{ key }}-affinity={{ value }}
{% endfor %}
{% for (key, value) in config.event_log.items() %}
--event-log-{{ key }}={{ value }}
{% endfor %}
{% for (key, value) in config.options.items() %}
--{{ key }}={{ value }}
{% endfor %}
