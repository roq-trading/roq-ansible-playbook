---

roq_bitstamp_defaults:
  version: "{{ roq_version }}"
  docker:
    use: "{{ roq_use_docker }}"
    name: "roqtrading/roq-bitstamp"
  service:
    start: "Sun *-*-* 00:01:00"
    stop: "Sat *-*-* 23:59:00"
  logging:
    verbosity: 0
  options:
    name: "bitstamp"
    config_file: "{{ root }}/usr/local/etc/roq-bitstamp/config.toml"
    event_log_dir: "{{ root }}/var/lib/roq/event-logs"
    client_listen_address: "{{ root }}/run/roq/roq-bitstamp.sock"
    metrics_listen_address: "{{ root }}/run/roq/metrics/roq-bitstamp.sock"
    ws_uri: "wss://ws.bitstamp.net"
    rest_uri: "https://www.bitstamp.net"
    fix_uri: "tcp+ssl://fix.bitstamp.net:8905"
    cache_mbp_allow_price_inversion: true
  symbols:
    - ".*"
