#!/usr/bin/env bash

set -e

ARGS="${@:-"--flagfile={{ directory_config.usr_local_etc }}/roq-influxdb/flags.cfg"}"

"{{ directory_config.opt }}/conda/bin/roq-influxdb" "{{ directory_config.var_lib }}/roq/data" "$ARGS"
