---

anaconda:
  url: "https://repo.continuum.io"
  sources:
    - "defaults"

miniconda_sh: 'Miniconda3-latest-{{ "MacOSX" if ansible_os_family == "Darwin" else "Linux" }}-x86_64.sh'

roq:
  url: "https://roq-trading.com"
